
| 文件                      | 用到的 API / 模式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `map_protect.py`        | 插件生命周期（initGui/unload/run）、`QAction`、`iface.addPluginToMenu/addToolBarIcon`、`QIcon` 资源路径 `:/`、`QSettings`/`QTranslator` 国际化                                                                                                                                                                                                                                                                                                                                                                   |
| `map_protect_dialog.py` | `uic.loadUiType`、`QgsMapLayerProxyModel.PolygonLayer` 过滤、`QgsMapLayerComboBox`（currentLayer/setLayer/setExceptedLayerList）、`QgsFileWidget`（storageMode/filePath/fileChanged）、`QgsProject` 信号 layersAdded/layersRemoved、图层信号 `selectionChanged` + `selectedFeatures`/`selectByExpression`（选择工具联动）、`QTableWidget`（itemChanged/itemSelectionChanged/cellWidget/selectionModel().selectedRows/QItemSelection 程序化选行/ItemIsEditable 编辑模式）、`QMessageBox` 自定义确认/取消按钮、`blockSignals` 防递归、`QFileDialog` |
| `region_importer.py`    | `QgsVectorLayer` 打开/isValid、字段探测（`f.type() == QVariant.String`）、`QgsFields/QgsField` 构造、`QgsFeature` 构造与几何复制、`QgsVectorFileWriter.create` + `SaveVectorOptions`、`QgsFeatureSink.FastInsert`、dataProvider 原地重建（deleteFeatures/addFeatures）、`QgsProject` 图层查找与增删、`QgsFillSymbol.createSimple` + `QgsCategorizedSymbolRenderer` 分类渲染、`saveDefaultStyle`（`result[1]` 判定）、`triggerRepaint`、region_cache 缓存读写                                                                                       |
| `region_editor.py`      | 惰性索引构建（region_cache 优先）、`feature_from_entry` 条目懒构造、按 `feature.id()` 批量 `deleteFeatures`、`addFeatures` + `triggerRepaint`、"先图层后内存、失败回滚"原子模式、分类符号追加/移除（`renderer.classAttribute()`/`categories()`）                                                                                                                                                                                                                                                                                              |
| `region_cache.py`       | `bytes(geometry.asWkb())` 序列化 + `QgsGeometry().fromWkb()` 还原（3.16 实例方法）、签名失效（mtime/size）、pickle 缓存、条目→要素按字段名对齐构造                                                                                                                                                                                                                                                                                                                                                                              |
| `region_exporter.py`    | 无 PyQGIS（纯 Python：json/os），可作为逻辑解耦的范例                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `test/utilities.py`     | headless `QgsApplication` 初始化 + 伪造 `QgisInterface`，pytest 基础设施                                                                                                                                                                                                                                                                                                                                                                                                                                |